/**
 * Énumération des différentes disposition valides pour l'affichage des notifications toast
 */
export enum EToastLayout {

    BASELINE = 'baseline', // Ligne de base
    STACKED = 'stacked', // Empilé
}